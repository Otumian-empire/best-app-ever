import React from "react";

function Description(props) {
  return (
    <div id="desc" className="container">
      <h1>Applications</h1>
      <small>
        Each application provides a unique set of API keys and configuration
        settings for making requests to Square APIs.
      </small>
    </div>
  );
}

export default Description;
